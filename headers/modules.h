#ifndef MODULES_H
#define MODULES_H

#include <iomanip>
#include <sstream>
#include <string>

int timeToMinutes(const std::string& time);
std::string minutesToTime(int total_minutes);
#endif //MODULES_H
